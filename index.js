function balloons(order, prices){
	var orders = order.split(",");
			return orders;

				var total = [];

					for (var i= 0; i<prices.length; i++) {
						var totalAmount = prices[i] + prices.sum 
							return totalAmount;

								if(order === +2){
									return order;
								}
}
};

console.log(balloons());
